name: Deploy website

on:
  push:
    branches:
      - main
    paths:
      - "Website/public/**"
      - "docs/appcast.xml"
      - "docs/beta/appcast.xml"
      - "docs/release-notes/**"
      - "Documentation/ReleaseNotes/**"
      - "scripts/build_release_notes.py"
      - "scripts/stamp_site_version.py"
      - ".github/workflows/deploy-website.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to GitHub Pages
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Merge ShipHook appcast output
        run: |
          if [ -f docs/appcast.xml ]; then
            cp docs/appcast.xml Website/public/appcast.xml
          fi

          if [ -f docs/beta/appcast.xml ]; then
            mkdir -p Website/public/beta
            cp docs/beta/appcast.xml Website/public/beta/appcast.xml
          fi

      - name: Stamp released version into the site
        run: |
          # The landing page upgrades its version text and download link from the
          # GitHub API at runtime, but that fetch is rate limited and can fail; the
          # baked-in literals are what visitors see when it does. Stamp them from
          # the appcast so they cannot drift behind a release again.
          python3 scripts/stamp_site_version.py

      - name: Build release notes
        run: |
          # ShipHook writes to docs/release-notes. Curated pages live separately under
          # Documentation/ReleaseNotes so ShipHook never copies a file onto itself.
          # The builder merges both sources, preferring curated pages for the website.
          python3 scripts/build_release_notes.py --check
          python3 scripts/build_release_notes.py

      - name: Upload website
        uses: actions/upload-pages-artifact@v5
        with:
          path: Website/public

      - name: Deploy website
        id: deployment
        uses: actions/deploy-pages@v5
